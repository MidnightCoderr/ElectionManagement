/**
 * CandidateSelection.jsx
 * Re-exports CandidateSelector as the step-4 component.
 * The screenshot shows both "CandidateSelectio..." and "CandidateSelector..." —
 * this file is the page-level wrapper, CandidateSelector is the presentational list.
 */
export { default } from './CandidateSelector.jsx'
